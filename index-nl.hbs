<!DOCTYPE html>
<html lang="nl">
<head>

    {{!-- Document Settings --}}
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>

    {{!-- Base Meta --}}
    <title>waterstof voor sporters en gezondheid</title>
    <meta name="HandheldFriendly" content="True"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    {{!-- Styles'n'Scripts --}}
    <link rel="stylesheet" type="text/css" href="{{asset "main/css/main.css"}}"/>

    {{!-- This tag outputs SEO meta+structured data and other important settings --}}
    {{ghost_head}}

</head>
<body class="is-preload {{body_class}}">

<div id="wrapper">
    <main id="main">
        <div class="inner">

            {{!-- Header --}}
            <header id="header">
                {{#if @site.logo}}
                    <a href="{{@site.url}}/nl/" class="logo image"><img src="{{@site.logo}}"
                                                                        alt="{{t "alt logo"}}"/></a>
                {{else}}
                    <a href="{{@site.url}}/nl/" class="logo">{{t "alt logo"}}</a>
                {{/if}}
                <ul class="icons">
                    <li><a href="https://shop.acniti.com" class="icon fa-shopping-cart" target="_blank"
                           title="{{t "buy"}}"><span class="label">{{t "buy"}}</span></a>
                    </li>
                </ul>
            </header>
            {{#is "/nl/"}}
                <h1> the home thingy is working!</h1>
            {{/is}}

            {{#is "page"}}
                {{> page-nl}}
            {{else}}
                {{> home-nl}}
            {{/is}}

        </div>
    </main>
    {{> sidebar-nl}}
</div>


{{> scripts}}

{{!-- Ghost outputs important scripts & data with the {ghost_foot} tag
it should always be the very last thing before the closing body tag --}}
{{ghost_foot}}

</body>
</html>



