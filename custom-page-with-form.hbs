<!DOCTYPE html>
<html lang="en">
<head>
    {{!-- Document Settings --}}
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>

    {{!-- Base Meta --}}
    <title>{{meta_title}}</title>
    <meta name="HandheldFriendly" content="True"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    {{!-- Styles'n'Scripts --}}
    <link rel="stylesheet" type="text/css" href="{{asset "main/css/main.css"}}"/>
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>

    {{!-- This tag outputs SEO meta+structured data and other important settings --}}
    {{ghost_head}}

</head>

<body class="is-preload {{body_class}}">

<div id="wrapper">
    <main id="main">
        <div class="inner">

            {{!-- Header --}}
            <header id="header">
                {{#if @site.logo}}
                    <a href="{{@site.url}}" class="logo image"><img src="{{@site.logo}}" alt="{{t "alt logo"}}"/></a>
                {{else}}
                    <a href="{{@site.url}}" class="logo">{{t "alt logo"}}</a>
                {{/if}}
                <ul class="icons">
                    <li><a href="https://shop.acniti.com" class="icon fa-shopping-cart" target="_blank"
                           title="{{t "buy"}}"><span class="label">{{t "buy"}}</span></a></li>
                </ul>
            </header>
            <section>

                <header class="main content">
                    <h1>Contact</h1>
                </header>

                <div class="content">
                    <p>First do the reCAPTCHA check, then write your message</p>
                    <p>Let us know your questions or give your ideas</p>

                    <form class="contact-form" action="https://formspree.io/{{> secret-email}}" method="post">
                        <div class="g-recaptcha" data-sitekey="6LfW4s8ZAAAAAJM9ZLZJxLyNLygXAbelQGMo0DVY"></div>
                            <label for="full-name">{{t "name"}}</label>
                            <input type="text" name="name" id="full-name" placeholder="{{t "first and last"}}" required>
                            <label for="email-address">{{t "email address"}}</label>
                            <input type="email" name="_replyto" id="email-address" placeholder="{{t "your email address"}}" required>
                            <label for="message">{{t "message"}}</label>
                            <textarea rows="5" name="message" id="message" placeholder="{{t "your message"}}" required></textarea>
                            <input type="hidden" name="_subject" id="email-subject" value="Contact Form Submission">
                            <input type="submit" value="{{t "submit"}}">
                    </form>
                </div>


            </section>
        </div>
    </main>
    {{> sidebar}}
</div>
{{!--load java script--}}
{{> scripts}}
{{!-- Ghost outputs important scripts & data with the {ghost_foot} tag
it should always be the very last thing before the closing body tag --}}
{{ghost_foot}}

</body>

</html>






